# Dependencias de la fase 1. En las semanas 2-3 se agregan: redis, arq, pywebpush, openai/anthropic, alembic.
[project]
name = "lyra-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy[asyncio]",
  "asyncpg",
  "aiosqlite",
  "pydantic-settings",
  "httpx",
]
